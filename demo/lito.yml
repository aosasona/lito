root:
  host: http://localhost
  port: 8080
services:
  - app-one:
      name: app-one
      host: localhost
      port: 8084
      health:
        path: /status
  - app-two:
      name: app-two
      host: localhost
      port: 8085
      health:
        path: /status
